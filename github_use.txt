## Upload to github...
git remote add origin "url"
git remote set-url origin "url"
git remote -v
git push -u origin main

# Problems with file ownership
takeown /f filename /r

# Pull and push
git fetch (descarga historial)
git pull (descarga historial y cambios)
git pull origin main

# Check
git clone
